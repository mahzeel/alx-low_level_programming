#include "main.h"

/**
 * mul - multiply 2 int
 * @a: a - Variable (first int)
 * @b: b - variable (second int)
 * Return: Always 0 (addition of the multiplication)
 */

int mul(int a, int b)
{
	return (a * b);
}
